pipeline {

    agent {
        dockerfile {
        filename 'docker/Dockerfile'
        }
    }
    stages {
        stage("build") {
            steps {
                sh '''source /opt/build.sh
                '''
            }
        }
    }
}